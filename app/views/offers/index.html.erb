
    <h1>Offers</h1>
    <% @offers.each do |offer| %>
      <%= link_to offer.name, offer_path(offer) %>
      <h3><%= offer.name %></h3>
      <p><%= offer.description %></p>
      <p>Amount: <%= humanized_money_with_symbol(offer.price) %></p>

    <% end %>

